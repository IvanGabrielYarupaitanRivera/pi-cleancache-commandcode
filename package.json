{
  "name": "pi-cleancache-commandcode",
  "version": "1.0.0",
  "description": "Pi custom provider for CommandCode API — cache-optimised bridge via static context (CleanCache)",
  "keywords": ["pi-package", "pi-provider", "commandcode", "deepseek", "prefix-caching", "cache-clean"],
  "type": "module",
  "license": "MIT",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@earendil-works/pi-ai": "latest",
    "@earendil-works/pi-coding-agent": "latest",
    "typebox": "^1.3.1"
  }
}
